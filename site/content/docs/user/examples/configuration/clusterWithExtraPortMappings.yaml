kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30950
        # if port is used by ANOTHER process -> switch it
        #hostPort: 80
        hostPort: 8080